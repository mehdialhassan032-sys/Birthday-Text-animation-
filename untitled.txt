<!DOCTYPE html>
<html lang="hi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>K Browser</title>
  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    body {
      background-color: #202124;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      padding: 20px;
    }

    /* Browser Window Frame */
    .browser-container {
      width: 100%;
      max-width: 1000px;
      height: 600px;
      background: #292a2d;
      border-radius: 10px;
      overflow: hidden;
      display: flex;
      flex-direction: column;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
    }

    /* Tab Bar */
    .tab-bar {
      display: flex;
      background-color: #1f1f1f;
      padding: 8px 8px 0 8px;
      gap: 5px;
    }

    .tab {
      background: #292a2d;
      color: #e8eaed;
      padding: 8px 16px;
      border-radius: 8px 8px 0 0;
      font-size: 13px;
      cursor: pointer;
      display: flex;
      align-items: center;
      gap: 8px;
      max-width: 180px;
    }

    .tab.active {
      background: #35363a;
    }

    /* Navigation / Toolbar */
    .toolbar {
      display: flex;
      align-items: center;
      gap: 10px;
      background-color: #35363a;
      padding: 8px 12px;
    }

    .nav-btn {
      background: transparent;
      border: none;
      color: #e8eaed;
      font-size: 16px;
      cursor: pointer;
      padding: 6px;
      border-radius: 50%;
      width: 32px;
      height: 32px;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .nav-btn:hover {
      background: rgba(255, 255, 255, 0.1);
    }

    /* Address / Search Bar */
    .address-bar {
      flex: 1;
      background: #202124;
      border: 1px solid #5f6368;
      border-radius: 20px;
      padding: 6px 16px;
      color: #fff;
      font-size: 14px;
      outline: none;
    }

    .address-bar:focus {
      border-color: #8ab4f8;
    }

    /* Web View Container */
    .content-area {
      flex: 1;
      background: #fff;
    }

    iframe {
      width: 100%;
      height: 100%;
      border: none;
    }
  </style>
</head>
<body>

  <div class="browser-container">
    <!-- Tabs Header -->
    <div class="tab-bar">
      <div class="tab active">
        <span>K Browser Home</span>
      </div>
    </div>

    <!-- Navigation Bar -->
    <div class="toolbar">
      <button class="nav-btn" id="back-btn" title="Back"></button>
      <button class="nav-btn" id="forward-btn" title="Forward"></button>
      <button class="nav-btn" id="reload-btn" title="Reload"></button>
      <input type="text" id="url-bar" class="address-bar" value="https://bing.com">
      <button class="nav-btn" id="go-btn" title="Go"></button>
    </div>

    <!-- Web Content View -->
    <div class="content-area">
      <iframe id="web-view" src="https://bing.com"></iframe>
    </div>
  </div>

  <!-- JavaScript Logic -->
  <script>
    const urlBar = document.getElementById('url-bar');
    const webView = document.getElementById('web-view');
    const goBtn = document.getElementById('go-btn');
    const reloadBtn = document.getElementById('reload-btn');

    // Navigation Function
    function loadUrl() {
      let url = urlBar.value.trim();

      // Check if user entered URL or Search Term
      if (!url.startsWith('http://') && !url.startsWith('https://')) {
        if (url.includes('.') && !url.includes(' ')) {
          url = 'https://' + url;
        } else {
          // Search query via Bing/Google
          url = 'https://www.bing.com/search?q=' + encodeURIComponent(url);
        }
      }

      urlBar.value = url;
      webView.src = url;
    }

    // Event Listeners
    goBtn.addEventListener('click', loadUrl);

    urlBar.addEventListener('keypress', (event) => {
      if (event.key === 'Enter') {
        loadUrl();
      }
    });

    reloadBtn.addEventListener('click', () => {
      webView.src = webView.src;
    });
  </script>

</body>
</html>
